# Claude Opus 審查處理 — 2026-09-16

審查由本機 Claude CLI 的 `--model opus --effort high --safe-mode --tools ''` 執行，回傳模型為 **claude-opus-5**。第一輪沙盒呼叫因 DNS 失敗，重新連線後完成。原始審查見同目錄 `opus-review-2026-09-16.md`；引用行號對應送審快照，不是目前版本。

## 已修正

- P0 切題截斷：Student 固定進行中的錄音題目，直到錄完與送出才切換。Chrome 實測老師在錄音時收題，WAV 仍接近完整 10 秒且不超限。
- P1 逾時鎖死：仍開放的題目允許更新未提交 ticket，恢復錄音；已提交不會重新評分。
- P1 批次重試：教師可一次重試失敗及超過 90 秒仍處理中的錄音。重試仍保留原子狀態比對，防止重複執行。
- P1 成本界線：以資料庫鎖與 trigger 限制每課 150 人、80 題；錯誤教師密碼延遲 1 秒回應。不是完整的網際網路濫用防護，課堂碼仍應限班內分享。
- P1 無效連結／身分：課堂不存在與加入失效分開回覆；學生身分失效可回到加入表單。
- P1 刪除：老師可刪除整堂課及私有錄音。先結束課堂、阻擋未提交錄音；仍在評分時待處理完成再刪。
- P1 測試：新增混合學生狀態、未答篩選、批次重試及 CSV 匯出測試；線上測試 ticket 恢復、151 人拒絕、收題後既有錄音提交、靜音及刪除。
- P2 音訊：AudioContext 明確使用 16 kHz，避免低採樣率轉換問題。
- P2 排序／更新：題目以 position + id 排序；老師回應使用序號丟棄過時刷新，成功刷新清除網路錯誤。
- P2 CSV／播放：防護公式前的空白／控制字元，淨化檔名；播放網址失效提示重新取得。

## 依證據保留的設計

審查對 `responseFormat.text.mimeType = APPLICATION_JSON` 的懷疑沒有沿用。此格式已通過本專案真實 Gemini 呼叫；小寫 MIME 反而收到明確 HTTP 400 列舉錯誤。模型名稱另外改用 InterAct 的即時模式設定，避免舊通用設定格式錯誤。參考官方結構化輸出文件，並以端點的實際回應驗證。

只去除完整 JSON 外圍的 Markdown 圍欄；不從任意文字抽取第一段 JSON、不接受超範圍或缺少的數值分數冒充有效評分。格式無效保留錄音並允許重試。

額外發現並修正：Gemini 曾對完全靜音捏造回答，因此先在伺服器檢查 PCM 音量，靜音直接標為無法判讀，不送 AI；以線上靜音驗證確認。AI 忙碌時有備援模型，空回饋有預設提示。

## 仍需實體課堂驗證／後續範圍

- 真實 iPhone Safari、Android Chrome 的麥克風、藍牙耳機和校園網路尚未實測。Chrome 的手機尺寸／合成麥克風測試不能替代真機。
- 尚未做 30–40 人同時呼叫 AI 的壓力測試；3 秒／4 秒輪詢與批次重試已有實作。
- 第一版只列已加入的學生；沒有修課名單匯入、強身分驗證。
- 上傳失敗的錄音保留於當前頁面記憶體；重新整理可能失去該段錄音，但未提交且仍開放的題目可以再錄。
- 手動刪除已實作；沒有自動保存期限或定期清除。

本紀錄為實作者逐項處理結果，不宣稱 Opus 已再次獨立複審修正後版本。
